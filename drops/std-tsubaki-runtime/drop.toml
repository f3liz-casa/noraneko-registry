# library drop: Tsubaki の runtime(wasm_of_ocaml の glue + WasmGC)。使う drop の child が sandbox で起こして ctx.ops にする。
# wasm/ は build の産物で、**この repo の中で組んだもの**(scripts/build-wasm.rb)。
# どの commit から、どう組んだか、どの toolchain で通したかは wasm.toml に書いてある。
# verify が sha256 を見るので、黙って差し替わらない。CI から組み直すのは、素の枝が
# push されたら。
# .tsb の形は TSB3(引数が `...` を持てるようになった版)。畳む道具も同じ commit のもの。
#
# 0.8.0 から、走らせる側は **ソースを読まない**。読むのは build が畳んだ命令列
# (ops.tsb)一枚。ctx.ops の口も `call` だけになる(`eval` と `load` は、読むものが
# 無くなったので消えた。誰も使っていなかった)。
# 畳むのは tooling/webext-actors/tsubakic、呼ぶのは build.ts の foldOps。
#
# 0.9.0 から、その VM は **Rust** で書かれている(nyanrus/tsubaki の tsbvm/)。
# import を一つも持たない素の wasm なので、glue も document のふりも要らない --
# worker はバイトを渡して、バイトを読み戻すだけ。`call` の引数と答えは JSON で
# 越える(closure は越えない。worker から出るものは構造化クローンされるので、
# もともと越えられない)。
# 互換性の基準は **本物の Julia**: 例えば `[1, 2, 3]` は `[1, 2, 3]` と出る
# (OCaml の runtime は float の並びで持っていたので `[1.0, 2.0, 3.0]` だった)。
#
# 0.16.0 から、その言葉は **ここから一枚だけ配る**。前は `ops/std.tsubaki` を使う
# 側の xpi に一枚ずつ写して、畳むときも drop のぶんと一緒くたにしていた(九枚の
# drop に、同じ 16,492 バイトが入っていた)。いまは畳むのも package ごとで、
# 走らせる側が **二枚読む** -- この lib の `ops.tsb` が先、drop 自身のがあと
# (tsbvm の `tsb_load` が、いまの global scope の上に足す)。
# .tsubaki は xpi の source/ に残るので、何が動くのかは今までどおり読める。

uuid = "8a0e317f-4d94-4917-aef4-ad26aac11653"
name = "std-tsubaki-runtime"
note = "Tsubaki(Julia の書き心地の小さな言語)の runtime と、その最初の言葉(ops/std.tsubaki: get(d, :key, 既定) / copy / put / merge、40px の単位、view と effect のことば、色の棚 Theme)。a[begin + i] と begin ... end が書ける。言葉は t(:鍵) で書けて、字は drop の strings.toml から(選ぶのは殻)。タブのことば(見ていてほしい出来事、目印と覚書、字を打つ欄)"
contact = ["gh/nyanrus"]
lib = true
version = "0.16.0"
