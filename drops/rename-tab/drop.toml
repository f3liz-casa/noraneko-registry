uuid = "ef4fa247-aeec-497c-8166-134af654cde9"
name = "rename-tab"
note = "タブに自分の名前をつける(タブを右クリック → Rename tab)。名前は pref に残る"
contact = ["gh/nyanrus"]
actors = ["rename-tab"]   # src/rename-tab/actor.ts。ブラウザの窓そのもの(browser.xhtml)に効く
