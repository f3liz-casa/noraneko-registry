// SPDX-License-Identifier: MPL-2.0

// Where noraneko's own tab-rename kept its names. Read once, so a name given
// before this drop carries over (see io/store.ts).
export const PREF_DATA = "noraneko.tabRename.data";

/** What the menu rows say. A drop has no i18n yet, so they say it in one language. */
export const MENU_RENAME = "Rename tab…";
export const MENU_CLEAR = "Clear name";
